[package]
name = "audio_bypass"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"] 

[dependencies]
godot = { version = "0.4.2", features = ["register-docs"] }
sdl3 = { version = "0.16.2", features = ["build-from-source-static"] }
